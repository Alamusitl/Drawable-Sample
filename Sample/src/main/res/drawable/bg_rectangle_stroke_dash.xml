<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <solid android:color="@color/colorPrimary"/>
    <corners android:radius="@dimen/shape_arc"/>
    <padding
        android:bottom="@dimen/shape_padding"
        android:left="@dimen/shape_padding"
        android:right="@dimen/shape_padding"
        android:top="@dimen/shape_padding"/>

    <!-- 设置描边 -->
    <!-- width 设置边框宽度 -->
    <!-- color 设置边框颜色 -->
    <!-- dashWidth 虚线时的横线长度 -->
    <!-- dashGap 虚线时的横线之间的距离 -->
    <stroke
        android:width="@dimen/shape_stroke_width"
        android:color="@android:color/darker_gray"
        android:dashGap="@dimen/shape_stroke_dash_gap"
        android:dashWidth="@dimen/shape_stroke_dash_width"/>
</shape>